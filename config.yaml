ollama:
  host: localhost
  port: 11434

mongo:
  host: localhost
  port: 27017

neo4j:
  host: localhost
  port: 7687

embedding_models:
  - nomic-embed-text

rag_models:
  - llama3.1

rag_enabled: true

unified_metadata:
  - title
  - section
  - subsection
